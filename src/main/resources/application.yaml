spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tubelens?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: pis3541!
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
#    properties:
#        hibernate:
#            dialect: org.hibernate.dialect.MySQL8Dialect
  logging:
    level:
      org.hibernate.SQL: debug